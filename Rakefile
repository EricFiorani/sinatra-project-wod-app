ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment'
require 'sinatra/activerecord/rake' #In Rakefile because you only need this in memory when you run a rake task. rake Rakefile, finds all rake tasks
